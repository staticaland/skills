{
  "name": "github-actions",
  "version": "0.1.0",
  "author": {
    "name": "staticaland"
  },
  "description": "Run GitHub Actions workflow steps in parallel, replace repeated workflow configuration with YAML anchors, and pin every action reference to a commit SHA on workflow edits"
}
