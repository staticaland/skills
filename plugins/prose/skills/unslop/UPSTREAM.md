# Upstream

Vendored from [cursor/plugins](https://github.com/cursor/plugins), path
`pstack/skills/unslop`, published by Lauren Tan under the MIT license
(`pstack/LICENSE`, copied here as `LICENSE`).

The commit this copy came from is in `vendir.yml` and `vendir.lock.yml`, so
no version is repeated here.
