# in-progress

Draft plugins and skills not yet ready to publish.

Nothing in this directory may appear in `.claude-plugin/marketplace.json` or the
top-level `README.md` catalog. When a draft is ready, move it to the repo root,
add it to both, and give it a version.
